#include "main.h"
/**
 *add - adds two integers
 *
 *Return: the result
 */
int add(int n, int x)
{
	int result = n + m;
	int frst = 0;
	int snd = 0;

	if (resut < 10)
	{
		_putchar('0' + result);
	}
	else
	{
		frst = result / 10;
		snd = result % 10;
		_putchar('0' + frst);
		_putchar('0' + snd);
	}
}
