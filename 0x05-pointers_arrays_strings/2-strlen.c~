#include "main.h"
/**
 *_strlen - give back the length of  string
 *@s: string
 *
 *Return: length of the given string
 */
int _strlen(char *s)
{
	int j = 0;

	while (*s[j] != '\0')
	{
		j++;
	}
	return (j + 1);
}
